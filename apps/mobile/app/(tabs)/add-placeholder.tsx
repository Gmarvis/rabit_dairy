// Placeholder route so the centre ＋ tab slot has something to register.
// It never renders — the tab press is intercepted to open the /add modal.
export default function AddPlaceholder() {
  return null;
}
